[project]
name = "transformers"
version = "0.1.0"
description = "Reusable data transformation functions for data pipelines"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
